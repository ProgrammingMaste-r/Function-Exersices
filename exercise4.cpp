//Write a C++ program to implement a recursive function to calculate the sum of digits of a given number.
#include <iostream>
#include <cmath>
using namespace std;

int digitSum(int num, int d)
{
    int temp = d;   
    if ( )
    return 0;
    return num % pow(10,4) + digitSum(num/ pow(10, 1 + temp), d );
}

int main ()
{

    return 0;
}